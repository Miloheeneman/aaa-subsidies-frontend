# Backend base URL. The /api/v1 suffix is optional — the axios client
# appends it automatically if it's not present. Both of these work:
#   VITE_API_URL=http://localhost:8000
#   VITE_API_URL=http://localhost:8000/api/v1
VITE_API_URL=http://localhost:8000
